export default function Tab({deletable, text}) {
    //TODO add deletable
    return (
        <div>
            <p style={{margin: "0.5em 0"}}><span class="Tab">{text}</span></p>
        </div>
    )
}